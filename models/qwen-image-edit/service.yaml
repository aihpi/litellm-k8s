apiVersion: v1
kind: Service
metadata:
  name: qwen-image-edit
  namespace: litellm
spec:
  selector:
    app: qwen-image-edit
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
