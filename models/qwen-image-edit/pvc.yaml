apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen-model-storage
  namespace: litellm
spec:
  volumeName: new-qwen-model-storage
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-k8s-general
  resources:
    requests:
      storage: 50Gi
