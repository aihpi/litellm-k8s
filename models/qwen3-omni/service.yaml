apiVersion: v1
kind: Service
metadata:
  name: qwen3-omni-service
  labels:
    app: qwen3-omni
spec:
  type: ClusterIP
  selector:
    app: qwen3-omni
  ports:
    - name: http
      port: 8000
      targetPort: 8000
