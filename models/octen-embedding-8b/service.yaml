apiVersion: v1
kind: Service
metadata:
  name: octen-embedding-8b-service
  labels:
    app: octen-embedding-8b
spec:
  type: ClusterIP
  selector:
    app: octen-embedding-8b
  ports:
    - name: http
      port: 8000
      targetPort: 8000
