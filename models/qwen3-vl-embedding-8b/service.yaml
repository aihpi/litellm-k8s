apiVersion: v1
kind: Service
metadata:
  name: qwen3-vl-embedding-8b-service
  labels:
    app: qwen3-vl-embedding-8b
spec:
  type: ClusterIP
  selector:
    app: qwen3-vl-embedding-8b
  ports:
    - name: http
      port: 8000
      targetPort: 8000
