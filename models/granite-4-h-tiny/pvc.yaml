apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: granite-4-h-tiny-cache
spec:
  volumeName: new-granite-4-h-tiny-cache
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-k8s-general
  resources:
    requests:
      storage: 50Gi
