apiVersion: v1
kind: Service
metadata:
  name: gpt-oss-120b-service
  labels:
    app: gpt-oss-120b
spec:
  type: ClusterIP
  selector:
    app: gpt-oss-120b
  ports:
    - name: http
      port: 8000
      targetPort: 8000
